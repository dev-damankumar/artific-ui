import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';
import {Divider} from '../../src';


<Meta
	title="Components/Divider"
	component={Divider}
	parameters={{docs: {source: {type: 'code'}}}}
/>

export const Template=(args) => {
	return <>
		<h5>Basic Divider</h5>
		<Divider/>
		<h5>Basic Divider</h5>
	</>
};

# Divider

The divider is used to divide section by putting line between them.

<Canvas>
	<Story name="Default">{Template.bind({})}</Story>
</Canvas>

## API

```jsx
import { Divider } from 'artific-ui';
```

<ArgsTable story="Default"/>

## Divider Direction

The Divider has main two directions ie. (`horizontal`|`vertical`)

<Canvas>
	<Story name="Containers">
		<div style={{display: 'flex', height: 200}}>
			<div style={{width: 200, background: 'gainsboro'}}>Basic Divider</div>
			<Divider direction="vertical"/>
			<div style={{width: 200, background: 'gainsboro'}}>Basic Divider</div>
		</div>
	</Story>
</Canvas>


